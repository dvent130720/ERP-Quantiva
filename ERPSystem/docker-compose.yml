version: '3.9'
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: erp_facturacion
      POSTGRES_USER: erp_user
      POSTGRES_PASSWORD: diego123
 #   ports:
#      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
